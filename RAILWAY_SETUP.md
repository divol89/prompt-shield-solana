# Railway Deployment Configuration

This file tells Railway how to run our Shield API.

- **Build Command**: `npm install && npm run build`
- **Start Command**: `npm start`
- **Environment Variables Needed**:
  - `PORT`: 4000
  - `NODE_ENV`: production
  - `SHIELD_ADMIN_KEY`: (Set this in Railway dashboard for your private access)
